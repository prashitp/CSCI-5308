image: maven:latest
variables:
  MAVEN_BUILD: "./mvn clean compile"
  MAVEN_TEST: "./mvn test"

stages:
  - build
  - test

build:
  stage: build
  tags:
    - ugrad
  script:
    - $MAVEN_BUILD

test:
  stage: test
  tags:
    - ugrad
  script:
    - $MAVEN_TEST
